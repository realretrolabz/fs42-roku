{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Roku Channel: sideload",
      "type": "brightscript",
      "request": "launch",
      "rootDir": "${workspaceFolder:Roku Channel}",
      "host": "${promptForHost}",
      "password": "${promptForPassword}",
      "stopOnEntry": false
    }
  ]
}
